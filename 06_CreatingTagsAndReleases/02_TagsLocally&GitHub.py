# 2'32

# "Branch points to a snapshot in the history of your project"

# Tag points to an important point in history (npr V1.0)

# in git 2 types of tags: Lightweight (just pointer to a commit); Annotated (kompletniji jer sadrzi poruku, email, date, checksum itd i generalno kad kreiramo tag kreiramo njega)

# Tags se mogu dodavati kasnije na postojeci commit

# Dodaje se (annotated) koristeci kdu
# ? git tag -a

# dok ih mozemo kreirati lokalno i pushati na gh, na gh imamo i ekstra uz tags i releases!!!

# kad code ima tag mozemo ga dld-ati as zip-fajl pozivajuci se na taj tag!

# na gh mozemo vidjeti commite koji su doveli do tekuceg commit-a kao i jos neke infose o commit-u
