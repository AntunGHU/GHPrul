# 5'34

# ? > cd projDir
# ? > git init (projDir - ako ga prije nismo kreirali)
# ulazimo u projDir i vidimo da sadrzi .git dir - lokalni db gita
# ulaskom u dir projDir prompt u termu se mjenja i dobija nastavak (master - u WinVSC jos uvjek!?!?)
# ? > git status
# kreira fajl
# ? > git status
# ? > git add "kreirani fajl"
# ? > git status
# ? > git commit -m "PrviCommit"
# ? > git status
# kreirao nekoliko fajlova i nakon
# ? > git status
# k-da za addanje svih
# ? > git add .
# ? > git status
# ? > git commit -m "ZajednickiCommit"
# ? > git status
# sve cisto
